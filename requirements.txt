# Requires Python 3.10-3.12 (CadQuery's OCC kernel lacks 3.13+ wheels).
# Pinned to the versions the cad-skill was last tested against.
cadquery>=2.6.1,<3
trimesh>=4.0,<5
rtree>=1.0,<2          # enables trimesh contains()/signed_distance for wall-solidity probes
pyrender>=0.1.45,<0.2
Pillow>=10,<13
numpy>=1.24,<3
matplotlib>=3.7,<4     # 2D side-profile blueprint render (skills/profile_blueprint.py)
lxml>=4.9,<7
